services:
  - type: web
    name: guitar-chords-web
    runtime: node
    plan: free
    autoDeployTrigger: commit
    buildCommand: npm install && npm run db:generate && npm run db:migrate && npm run db:seed && npm run build
    startCommand: npm run start
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: DIRECT_URL
        sync: false
      - key: ENABLE_DATABASE_READS
        value: "true"
      - key: SEED_DEMO_DATA
        value: "true"
